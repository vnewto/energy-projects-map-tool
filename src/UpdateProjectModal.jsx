export default function UpdateProjectModal({toggleUpdateModal}) {
  return (
    <div>
      <p>This will be the update project modal.</p>
      <button type='button' onClick={toggleUpdateModal}>Cancel</button>
    </div>
  );
}
